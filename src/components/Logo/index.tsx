import { LogoProps } from '@lobehub/ui/es';
import Image from 'next/image';

export const Logo = ({ className, extra, size = 36, type = 'text' }: LogoProps) => {
  return (
    // <Image alt="logo" className={className} height={size*0.7} src="/images/logo-text.png" width={size*2} />
    <></>
  );
};
